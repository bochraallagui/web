{% extends 'baseFront.html.twig' %}

{% block body %}
    <h1>Reclamation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdRec</th>
                <td>{{ reclamation.idRec }}</td>
            </tr>
            <tr>
                <th>Objectif</th>
                <td>{{ reclamation.objectif }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ reclamation.text }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reclamation_front_index') }}">Retour a la liste</a>

    <a href="{{ path('app_reclamation_front_edit', {'idRec': reclamation.idRec}) }}">Modifier</a>

    {{ include('reclamation_front/_delete_form.html.twig') }}
{% endblock %}
